title: OpenSearch Editorial Guides
description: Style, writing, and formatting standards for OpenSearch documentation contributors
remote_theme: just-the-docs/just-the-docs

url: ""
baseurl: ""

# Navigation order is controlled by front matter nav_order in each file
# Exclude files not needed in the build
exclude:
  - _config.yml
  - README.md
